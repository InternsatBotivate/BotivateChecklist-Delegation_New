// RealtimeLogoutListener — Supabase removed, no realtime features needed.
// This component is kept as a no-op to avoid breaking App.jsx imports.

const RealtimeLogoutListener = () => {
  return null;
};

export default RealtimeLogoutListener;
